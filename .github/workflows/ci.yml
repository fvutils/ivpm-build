name: CI
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  ci-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install dev dependencies
      run: |
        pip install -e ".[dev]"

    - name: Run Tests
      run: |
        python -m pytest test/ -v --tb=short \
          --cov=src/ivpm_build --cov-report=term-missing

    - name: Build Package
      run: |
        pip install build
        rm -rf dist/
        sed -i -e "s/^SUFFIX = \"\"$/SUFFIX = \".${GITHUB_RUN_ID}\"/" \
          src/ivpm_build/_version.py
        python -m build .

    - name: Build Docs
      run: |
        pip install sphinx sphinx-rtd-theme sphinx-argparse myst-parser
        sphinx-build -M html ./docs/source build
        touch build/html/.nojekyll

    - name: Publish to PyPI
      if: ${{ startsWith(github.ref, 'refs/heads/main') }}
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        user: __token__
        password: ${{ secrets.PYPI_API_TOKEN }}

    - name: Publish Docs
      if: ${{ startsWith(github.ref, 'refs/heads/main') }}
      uses: JamesIves/github-pages-deploy-action@4.1.7
      with:
        branch: gh-pages
        folder: build/html
